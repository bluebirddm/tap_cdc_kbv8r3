large_orders_sync:
  sql: "SELECT * FROM orders WHERE id > ? ORDER BY id LIMIT :chunkSize"
  index: kingbase_orders_large
  id-column: id
  chunk-size: 10000
  fetch-size: 10000
  stream-results: true
