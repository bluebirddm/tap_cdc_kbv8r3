sample_orders_sync:
  sql: "SELECT * FROM orders WHERE id > ? ORDER BY id LIMIT :chunkSize"
  index: orders
  id-column: id
  incremental: false
  enabled: true
  chunk-size: 5000
  fetch-size: 5000
  stream-results: true
